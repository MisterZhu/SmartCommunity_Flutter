enum SCEnvironment {
  /// 研发环境
  develop,
  /// 预发环境
  pretest,
  /// 生产环境
  production
}

enum SCHomeFeatureStyle {
  /// 一张图片
  featureStyle1,
  /// 两张图片
  featureStyle2
}

enum SCLocationStatus {
  /// 未开启定位
  notOpen,
  /// 刷新定位
  reload,
  /// 定位成功
  success
}